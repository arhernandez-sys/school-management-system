"""Test suite for the SIS FastAPI backend.

Marker package so `tests` imports as a package and pytest rootdir resolution is
stable. See `conftest.py` for the shared fixtures (settings / client / db_session /
auth headers) and `tests/README` notes in conftest for the isolation strategy.
"""

from __future__ import annotations
