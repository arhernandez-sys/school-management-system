/**
 * Audit trail module (D45 §46, §53). Dean / Auditor / System Administrator.
 * Read-only by construction — there is no write endpoint behind it.
 */
export { AuditTrailPage, default } from './AuditTrailPage';
