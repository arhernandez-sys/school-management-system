import { ModulePlaceholder } from '@shared/components/ModulePlaceholder';

/**
 * Announcements feature module (Phase 7). Foundation placeholder only.
 * Co-locates components/, api/, hooks/, types per architecture §5 (currently empty).
 */
export function AnnouncementsPage() {
  return <ModulePlaceholder module="announcements" title="Announcements" />;
}

export default AnnouncementsPage;
