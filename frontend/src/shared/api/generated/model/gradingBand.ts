/**
 * Generated by orval v7.13.2 🍺
 * HAND-EDITED (D30 Phase 3): added `grade_point`. `npm run generate:api` is FORBIDDEN
 * in this repo — `frontend/openapi.json` is stale and covers 4 of 14 modules, so
 * regenerating would delete more than it adds. See RUNBOOK / plan §F.
 * School Management System API
 * OpenAPI spec version: 1.0.0
 */

/**
 * A single contiguous band. Read + write share this shape; on write the
service validates contiguity over 0..100 (no gaps/overlaps, schema §5).
 */
export interface GradingBand {
  /**
   * @minLength 1
   * @maxLength 8
   */
  letter: string;
  /**
   * @minimum 0
   * @maximum 100
   */
  min_score: number;
  /**
   * @minimum 0
   * @maximum 100
   */
  max_score: number;
  /**
   * The band's value on the 4.00 scale (D30 §D5) — what makes a credit-weighted GPA
   * possible. Nullable: a scale predating Phase 3, including every archived year's
   * frozen scale, carries NULL, and NULL means "this scale cannot price this letter"
   * rather than 0.00 (which is an F).
   * @minimum 0
   * @maximum 4
   * @nullable
   */
  grade_point?: number | null;
  is_passing?: boolean;
  /** @minimum 0 */
  sort_order: number;
}
