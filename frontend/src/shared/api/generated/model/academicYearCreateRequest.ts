/**
 * Generated by orval v7.13.2 🍺 — then HAND-EDITED for D30 (N calendar terms per
 * year, plan §D3). `npm run generate:api` must NOT be re-run: `openapi.json` is stale
 * and covers 4 of 14 modules, so regenerating would delete far more than it restored.
 */
import type { SemesterCreateRequest } from './semesterCreateRequest';

/**
 * POST /settings/academic-years — creates the year and its terms.
 *
 * D30: **at least one** term, no longer exactly two. Sequences must be distinct; the
 * lowest one is the term made active.
 */
export interface AcademicYearCreateRequest {
  /**
   * @minLength 1
   * @maxLength 120
   */
  name: string;
  start_date: string;
  end_date: string;
  /**
   * @minItems 1
   * @maxItems 12
   */
  semesters: SemesterCreateRequest[];
}
