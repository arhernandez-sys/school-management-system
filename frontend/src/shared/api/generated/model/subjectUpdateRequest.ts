/**
 * Generated by orval v7.13.2 🍺 — then HAND-EDITED for D30 (the course-catalog
 * cutover, plan §D2). `npm run generate:api` must NOT be re-run: `openapi.json` is
 * stale and covers 4 of 14 modules, so regenerating would delete far more than it
 * restored. Regenerate this file only once the spec is refreshed.
 */
import type { CourseComponent } from './courseComponent';

/**
 * PATCH /subjects/{id}. All fields optional (partial update); an omitted field is
 * left alone. `code` cannot be cleared — the column is NOT NULL.
 */
export interface SubjectUpdateRequest {
  name?: string | null;
  code?: string | null;
  credits?: number | null;
  component?: CourseComponent | null;
  description?: string | null;
  prerequisites_text?: string | null;
  is_active?: boolean | null;
}
